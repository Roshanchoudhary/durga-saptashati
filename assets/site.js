(()=>{const t=document.getElementById("theme");if(localStorage.durgatheme==="dark")document.body.classList.add("dark");t?.addEventListener("click",()=>{document.body.classList.toggle("dark");localStorage.durgatheme=document.body.classList.contains("dark")?"dark":"light"});const list=document.getElementById("chapterList");if(list){fetch("/api/chapters").then(r=>r.json()).then(a=>{list.innerHTML=a.length?a.map(x=>`<a class="card" href="/chapter.html?id=${encodeURIComponent(x.id)}"><b>${String(x.chapter_number).padStart(2,"0")}</b>${x.title?`<span>${x.title}</span>`:""}<small>${x.subtitle||""}</small></a>`).join(""):`<div class="skeleton">Admin से अध्याय जोड़ें।</div>`}).catch(()=>list.innerHTML='<div class="skeleton">अभी कोई प्रकाशित अध्याय नहीं है।</div>')}})();